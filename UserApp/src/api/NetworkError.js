import React from "react";
import { View, Text } from "react-native";

const NetworkError = () => {
  return (
    <View>
      <Text>Network Error: Unable to connect to the server.</Text>
    </View>
  );
};

export default NetworkError;
